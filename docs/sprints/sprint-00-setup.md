# Sprint 00 — Setup

**Status:** complete

## Goal

Runnable project shell with docs, git, split-screen layout, placeholder art.

## Tasks

- [x] `git init`, `.gitignore`
- [x] Phaser 3 + Vite + TypeScript scaffold
- [x] `docs/GAME_DESIGN.md`, `ARCHITECTURE.md`, `ASSETS.md`, `SPRINTS.md`
- [x] `AGENTS.md`, `README.md`
- [x] `GameState` skeleton
- [x] `GameScene` with macro/micro panels + HUD
- [x] Queue P0 PixelLab assets

## Gate

- [x] `npm run dev` shows split screen + HUD
- [x] `npm run build` produces `dist/`

## Files touched

`package.json`, `src/*`, `docs/*`, `AGENTS.md`, `README.md`, `vite.config.ts`
